def find_anagram(word, anagram):
    if sorted(word) == sorted(anagram):
         return True
    else:
         return False
print(find_anagram("book", "silent"))
print(find_anagram("listen", "silent"))
